import { ApprainbowDirective } from './apprainbow.directive';

describe('ApprainbowDirective', () => {
  it('should create an instance', () => {
    const directive = new ApprainbowDirective();
    expect(directive).toBeTruthy();
  });
});
